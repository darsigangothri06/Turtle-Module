import turtle
# turtle is an object which creates then we import the turtle module.
# As soon as the object is created its position it set at (0,0), i.e. at the center of the turtle graphics window.

turtle.forward(20) # move turtle forward by 20 pixels.

